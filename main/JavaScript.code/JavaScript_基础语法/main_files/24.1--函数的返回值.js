/* 
    创建一个函数,用来创建三个数的和

    可以使用 return 来设置函数的返回值
    返回值可以是任意的数据类型,可以是对象,函数
    不设置 return 就会返回 Undefined

    return可以结束整个循环,其后的语句不会执行
*/

function sum(a,b,c) {

    var d = (a+b+c);

    return d;
}

console.log(sum(25,23,89));
console.log(sum(1,2,3));



